# Entorno y secretos (no subir claves ni URLs con credenciales)
.env
.env.*
!.env.example

# Node (mobile y cualquier carpeta futura con npm)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Java / Ant — salida de compilación
backend/build/
*.class

# Dependencias JDBC descargadas con `ant fetch-deps` (recrear con ese target)
backend/lib/*.jar

# IDE y SO
.idea/
*.iml
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs y temporales
*.log
*.tmp
*.swp
*~
